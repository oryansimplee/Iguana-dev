--Filtering messages, returning true if message was filtered and false if we wish to continue with the message
--shared requihttp://localhost:6543/mapper/res
require 'processmessage'


function main(data)
   
   processData(data, "pbar")   
end

